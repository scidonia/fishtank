.direnv/
result
**/__pycache__/
server/node_modules/

# Never commit plaintext secrets — only encrypted versions (env/secrets/*.env) are safe to commit
env/secrets/*.env.plaintext
env/local*.env
data/
