╔════════════════════════════════════════════════════════════════════╗
║                    FISH TANK - WHERE WE ARE                         ║
║                    Last Updated: 2026-02-12                         ║
╚════════════════════════════════════════════════════════════════════╝

✅ MVP WITH LARGE MAP AND VIEWPORT IS COMPLETE!

┌─────────────────────────────────────────────────────────────────────┐
│ QUICK START                                                          │
└─────────────────────────────────────────────────────────────────────┘

  ./start-demo.sh
  # Then open http://localhost:8080 and click "Connect"
  # In new terminals: uv run agent --agent-id a1

┌─────────────────────────────────────────────────────────────────────┐
│ WHAT WORKS NOW                                                       │
└─────────────────────────────────────────────────────────────────────┘

  ✓ 1000×1000 tile map loaded from ASCII file (shared/map.txt)
  ✓ Procedural dungeon with ~50 rooms and corridors
  ✓ Viewport-based rendering (only visible tiles rendered)
  ✓ Camera controls:
    - Click & drag to pan
    - Arrow keys to move
    - +/- buttons to zoom
    - Follow Agent button for auto-tracking
  ✓ Surveillance panel showing all agents
  ✓ Click any agent in sidebar to follow them
  ✓ World server with turn-based simulation
  ✓ SSE streaming (public + private agent observations)
  ✓ Agent runner (Python) with random walk AI
  ✓ Browser viewer with DCSS tile rendering
  ✓ Movement, collision detection, hunger, starvation
  ✓ Health bars, event log, tooltips
  ✓ Deterministic seeded RNG

┌─────────────────────────────────────────────────────────────────────┐
│ NEW FEATURES (Just Added)                                            │
└─────────────────────────────────────────────────────────────────────┘

  🆕 Large map support (1000×1000 instead of 25×18)
  🆕 Map loaded from ASCII file (shared/map.txt)
  🆕 Viewport rendering (performance optimized)
  🆕 Camera panning (click & drag, arrow keys)
  🆕 Zoom controls (+/- buttons)
  🆕 Agent following (auto-tracks selected agent)
  🆕 Surveillance panel with agent list
  🆕 Click agents in sidebar to follow them
  🆕 Camera position display
  🆕 Map size display in stats

┌─────────────────────────────────────────────────────────────────────┐
│ WHAT'S MISSING                                                       │
└─────────────────────────────────────────────────────────────────────┘

  ✗ Combat system (attack action)
  ✗ Food/items (forage, eat actions)
  ✗ Line-of-sight visibility (agents see full map now)
  ✗ LLM integration (DeepSeek v3)
  ✗ Agent memory system
  ✗ Determinism tests
  ✗ Security (auth, rate limiting)

┌─────────────────────────────────────────────────────────────────────┐
│ KEY FILES                                                            │
└─────────────────────────────────────────────────────────────────────┘

  STATUS.md                     ← READ THIS FIRST (comprehensive status)
  QUICKSTART.md                 ← User guide with examples
  DEVELOPMENT_CHECKLIST.md      ← Detailed implementation progress
  README.md                     ← Full documentation

  shared/map.txt                ← 1000×1000 ASCII map (978KB)
  shared/generate_map.py        ← Map generator script
  
  server/src/world.js           ← World simulation + map loading
  runner/main.py                ← Agent runner (170 lines)
  viewer/viewer.js              ← Canvas renderer with viewport (470 lines)
  shared/schemas.json           ← Protocol contracts

┌─────────────────────────────────────────────────────────────────────┐
│ NEXT STEPS (Priority Order)                                         │
└─────────────────────────────────────────────────────────────────────┘

  1. Combat system           → server/src/world.js
  2. Food/items system       → server/src/world.js + viewer/viewer.js
  3. Line-of-sight           → server/src/world.js
  4. LLM integration         → runner/main.py + new runner/llm.py
  5. Tests & logging         → New test files

┌─────────────────────────────────────────────────────────────────────┐
│ VIEWER CONTROLS                                                      │
└─────────────────────────────────────────────────────────────────────┘

  Mouse:
    • Click & Drag    → Pan camera
    • Hover           → Show entity info
  
  Keyboard:
    • Arrow Keys      → Move camera
  
  Buttons:
    • +/-             → Zoom in/out
    • Follow Agent    → Auto-track first agent
    • Agent in list   → Click to follow specific agent

┌─────────────────────────────────────────────────────────────────────┐
│ MAP GENERATION                                                       │
└─────────────────────────────────────────────────────────────────────┘

  To regenerate the map:
    cd shared
    python3 generate_map.py
  
  This creates a new 1000×1000 map with:
    • 50 randomly sized rooms (10-40 tiles each)
    • Corridors connecting all rooms
    • Random pillars for variety
    • ~6% floor space, ~94% walls

┌─────────────────────────────────────────────────────────────────────┐
│ ARCHITECTURE                                                         │
└─────────────────────────────────────────────────────────────────────┘

    Browser (:8080)     ←──SSE──     Server (:3000)     ──SSE→    Agent
    [Viewport Viewer]                [Turn Loop]                  [Python]
    • 1000×1000 map                  • Load map.txt                 │
    • Render viewport                • Find spawn points            │
    • Follow agents                  └──────── POST /act ───────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ TESTING                                                              │
└─────────────────────────────────────────────────────────────────────┘

  # Start everything
  ./start-demo.sh

  # Check server health
  curl http://localhost:3000/health

  # Run multiple agents
  uv run agent --agent-id a1 &
  uv run agent --agent-id a2 &

  # Watch in browser
  open http://localhost:8080
  
  # In viewer:
  • Click "Connect"
  • Drag to pan around the large map
  • Use +/- to zoom
  • Click "Follow Agent" to auto-track
  • Click agents in sidebar to follow specific ones

┌─────────────────────────────────────────────────────────────────────┐
│ PERFORMANCE                                                          │
└─────────────────────────────────────────────────────────────────────┘

  Viewport rendering ensures good performance even with 1000×1000 map:
  • Only visible tiles are rendered each frame
  • Efficient camera culling
  • Smooth 60 FPS rendering
  • No lag with multiple agents

┌─────────────────────────────────────────────────────────────────────┐
│ PROGRESS: 65% COMPLETE (Large map + viewport done!)                │
└─────────────────────────────────────────────────────────────────────┘

  ████████████░░░  65%  Large map support complete!


┌─────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                      │
└─────────────────────────────────────────────────────────────────────┘

  Port already in use error?
    ./stop-all.sh

  Check what's running:
    lsof -i:3000  # World server
    lsof -i:8080  # Viewer

  Check logs:
    tail -f /tmp/fishtank-server.log
    tail -f /tmp/fishtank-viewer.log

  Verify server is up:
    curl http://localhost:3000/health

